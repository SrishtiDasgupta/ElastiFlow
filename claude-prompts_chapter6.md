SeisSol Driver — execution details:
> Read the SeisSol/TinyDA driver script and the UM-Bridge integration code. Report: (1) how the Driver maps allocated nodes to chains — does it call the load balancer directly or receive assignments from it? (2) the exact sequence of operations in the EXECUTE phase: how Waitress endpoints are started, how TinyDA issues requests across chains, and how results are aggregated; (3) how convergence is assessed and how the Driver signals termination to the Engine; (4) the Singularity container startup sequence and any measurable cold-start overhead; (5) whether the Driver determines chains itself or receives it from TinyDA's output.

HPO Driver — Ray Tune integration
> Read the HPO driver script and Ray Tune integration code. Report: (1) how the Driver launches the Ray cluster on the allocated resources and what parameters it passes; (2) how the ASHA successive-halving promotion criterion is configured — specifically the keep-floor fraction and how parallelism(chains) is derived from it; (3) how the Driver signals convergence (no more trials to promote); (4) the dataset pre-caching mechanism and the file-lock implementation; (5) how the Driver communicates the core-level resource map to Ray's resource manager; (6) the exact per-trial core count used in experiments.

Coherence threshold — concrete values
> Search constants.py, constants_HPO.py, and the load balancer implementation for: (1) the concrete value of the coherence threshold (may also be called "closeness" or a ratio parameter) for both the SeisSol and HPO use cases; (2) the speedup threshold  used in resource shape pruning (reported as 1.4); (3) the exact greedy assignment algorithm used in the load balancer — confirm whether it is a least-loaded-bundle assignment as described in the briefing above or a different strategy.

License-Aware Execution
> This section covers the license-aware extension of ElastiFlow. Read any license-related scheduler files, the License Manager/Server integration code, and relevant constants. Report: (1) which commercial software tools require license tokens (e.g., LS-Dyna, Abaqus, Ansys); (2) how license availability is checked — is there a separate License Server queried by the Resource Manager, or is license tracking internal to the Resource Manager? (3) how license count is declared in the workflow YAML specification; (4) whether any software is restricted from cloud execution by licensing terms, and how this restriction is enforced at admission; (5) how license tokens are reserved and released relative to resource allocation and release; (6) the concrete extension to the feasibility check — what additional condition is evaluated in Stage 1.
